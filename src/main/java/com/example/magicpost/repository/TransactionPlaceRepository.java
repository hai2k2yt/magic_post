package com.example.magicpost.repository;

import com.example.magicpost.model.TransactionPlace;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TransactionPlaceRepository extends JpaRepository<TransactionPlace, Long> {
}
